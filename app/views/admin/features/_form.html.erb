<%= simple_form_for([:admin, @feature]) do |f| %>
    <table class="table">
      <tr>
        <td>Feature Name:</td>
        <td><%= f.input_field :name, class: 'span10' %></td>
      </tr>
      <tr>
        <td></td>
        <td><%= f.button :submit, class: 'btn btn-success' %> <%= link_to 'Cancel', :back, class: 'btn btn-small btn-default' %></td>
      </tr>
    </table>
<% end %>