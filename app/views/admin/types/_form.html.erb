<%= simple_form_for([:admin, @type]) do |f| %>
    <table class="table">
      <tr>
        <td>Name:</td>
        <td><%= f.input :name %></td>
      </tr>
      <tr>
        <td colspan="2"><%= f.button :submit %></td>
      </tr>
    </table>
<% end %>
